Preludes: list of things that Rust automatically imports into every Rust program.(helps us use features like: fn, println!, and so on.)
Module inside Crate, Package, Library.
use std::io; standard library(crate)::Input Output(Module)
::path seperator operator
stdin: Standard output in
expect: used to handle result values
