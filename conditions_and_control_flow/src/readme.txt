comparing values of different type gives error "mismatched types".
to prevent this error use type conversion.Eg: int to float.
Compound Condition work with boolean values.
Precedence: () > ! > && > ||;
Control Flow: if, else, else if.